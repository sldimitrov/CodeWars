def stringy(size):
    if size % 2 != 0:
        change = size % 2
        change = '1'
        returning_string = '10' * (size // 2) + change
        return f"{returning_string}"
    else:
        returning_string = '10' * (size // 2)
        return f"{returning_string}"
